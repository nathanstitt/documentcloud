<%
  @title = "Admin Dashboard :: Membership Applications"
  @stylesheets = [:admin]
  @javascripts = [:admin]
  @bodyclass   = 'pending_memberships'
%>

<script type="text/javascript">
  $(function(){
    $('#content').append( (new dc.ui.PendingMemberships( {
      collection: new dc.model.PendingMembershipSet( <%= PendingMembership.sorted.to_json %> )
    } ) ).render().el);
  });
</script>
